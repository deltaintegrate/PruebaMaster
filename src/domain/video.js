export class Video {
  constructor(id, titulo, descripcion, url, autorId) {
      this.id = id;
      this.titulo = titulo;
      this.descripcion = descripcion;
      this.url = url;
      this.autorId = autorId;
  }
}