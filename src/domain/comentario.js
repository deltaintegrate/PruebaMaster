export class Comentario {
  constructor(id, contenido, usuarioId, videoId) {
      this.id = id;
      this.contenido = contenido;
      this.usuarioId = usuarioId;
      this.videoId = videoId;
  }
}